# PostgreSQL Database URL (Render provides this automatically)
# Example for local development: postgresql+asyncpg://user:password@localhost/calendar_mcp
DATABASE_URL=

# A secure, random key for API authentication.
# The render.yaml is configured to generate this automatically on deployment.
CALENDAR_MCP_SERVER_API_KEY=your-secure-api-key-here

# --- Admin Panel Configuration ---

# A secure, random key for Flask session management.
# The render.yaml is configured to generate this automatically on deployment.
FLASK_SECRET_KEY=

# Credentials for the admin panel login.
# The render.yaml sets a default username and generates a secure password.
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your-admin-password

# --- Google OAuth Configuration (Required for Admin Panel) ---
# Get these from the Google Cloud Console for your project.
# These must be set in the Render dashboard for the 'calendar-mcp-admin' service.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# The full URL to the OAuth callback endpoint on your deployed admin panel.
# Example: https://calendar-mcp-admin.onrender.com/oauth2callback
REDIRECT_URI=
